# Under the same sky — V13 Starlit Gallery

Một món quà sinh nhật riêng đi từ Quy Nhơn đến Gothenburg.

## V13

- Bổ sung chòm sao, quỹ đạo, mây sáng và sao băng cho màn “Cùng một bầu trời”.
- Tinh gọn triển lãm còn năm ảnh.
- Bỏ “Dưới những ánh đèn” và ảnh sen đỏ khỏi triển lãm.
- Đưa ảnh selfie lên vị trí thứ hai với tên mới “Nụ cười anh muốn giữ”.
- Khung lời nhắn mặc định chỉ hiện tên ảnh; bấm trái tim niêm phong để mở nội dung.
- Thêm hoa nét mảnh, quầng sáng và hiệu ứng mở niêm phong cho khung lời nhắn.

## V12

- Tạo khoảng thở an toàn giữa chữ “Duyên” và lời nhắn màu đỏ ở hero.
- Thêm cành hoa nét mảnh, quầng sáng và những điểm lấp lánh nhẹ vào nền hero.
- Tinh giản màn “Cùng một bầu trời” còn hai địa điểm, hai đồng hồ, Anh/Em và một câu chốt.
- Giữ nguyên trái tim mở khóa, ảnh, gallery, game, lá thư và toàn bộ hiệu ứng trước đó.

## V11

- Đổi lời mở khóa thành “Mở bằng ngày anh luôn nhớ.”
- Thêm trái tim đập, quầng sáng và tia lấp lánh ở màn nhập mã.
- Trái tim phản ứng khi nhập mã và sáng lên khi mở khóa thành công.
- Tăng nhẹ mật độ hoa anh đào ở hero.
- Thay ảnh hai cái bóng bằng ảnh sen đỏ đã chọn.
- Giữ nguyên gallery, game, lá thư, giọng đọc dự phòng và toàn bộ hiệu ứng còn lại.

## V10.8

- Sửa hai tài nguyên hoa hồng bị thiếu bằng CSS nhẹ.
- Thêm favicon riêng.
- Thêm ảnh preview khi chia sẻ liên kết.
- Thêm Open Graph, Twitter Card, canonical URL và mô tả chia sẻ.
- Thêm `robots.txt` và `noindex` để hạn chế công cụ tìm kiếm lập chỉ mục.
- Thêm skip link, trạng thái voice có `aria-live`, vùng bấm tối thiểu 44px và focus trap cho modal.
- Giữ nguyên gallery, game, lá thư và đoạn kết của V10.7.

## Audio bổ sung sau

Đặt hai file vào `assets/`:

- `our-song.mp3`
- `message-for-duyen.mp3`

## Deploy

GitHub Pages: branch `main`, folder `/(root)`.

`https://enzoreacher.github.io/under-the-same-sky/`


## V10.8.1

- Sửa màn “Đọc dòng cuối cùng” xuất hiện ngoài viewport khi cinematic đã cuộn xuống.
- Epilogue bám trực tiếp theo viewport trên desktop và mobile.
- Khi quay lại, cinematic trở về đúng vị trí tấm vé.


## V10.9

- Cập nhật canonical, Open Graph và ảnh chia sẻ sang `https://under-samma-himmel.hoangnt2000.chatgpt.site/`.
- Trong toàn bộ ngày 12/8 theo múi giờ Gothenburg, đồng hồ chuyển thành lời chúc sinh nhật riêng.
- Sau ngày 12/8, đồng hồ tự chuyển sang sinh nhật năm kế tiếp.
- Làm mềm các đoạn kết và lời nhắn về tương lai để giữ sự chân thành mà không áp đặt một happy ending.
- Giữ nguyên vị trí chờ `our-song.mp3` và `message-for-duyen.mp3`.


## V15 — Soft Letters

- Dựng lại từ bản V13 ổn định; giữ nguyên toàn bộ JavaScript và dữ liệu ảnh.
- “Mở lúc cần” chuyển sang phong cách thiệp giấy tối giản, nhẹ và tình cảm hơn.
- Bỏ hoa hình vòng tròn/bullseye và phần ghi âm bên ngoài bị trùng; lời chúc vẫn nằm trong P.S. của lá thư.
- Giữ nguyên 5 ảnh triển lãm, đồng hồ hai múi giờ, mini game, PIN và các hiệu ứng đã hoàn thiện.


## V16 — Luminous Finale

- Bỏ hoàn toàn bó hoa ở góc và trái tim đỏ khổng lồ phía sau lời chúc.
- Thay bằng tấm thiệp kính mờ viền mảnh, dải lụa ánh hồng, đốm sáng và nét lá chuyển động nhẹ.
- Giữ nguyên thao tác “giữ để mở”, toàn bộ JavaScript, 15 ảnh nhúng và mọi phần ổn định của V15.
# V17 — Shadow Promise

- Chỉ thay ảnh số 01 trong đoạn kết điện ảnh bằng ảnh hai cái bóng của hai đứa.
- Giữ nguyên bộ ảnh triển lãm và toàn bộ luồng tương tác của V16.
- Rút gọn lời kết, thêm câu “Anh yêu em / Jag älskar dig” và một lời hẹn nhẹ về tương lai.
# V18 — Candlelit Cake

- Thêm một chiếc bánh sinh nhật nét mảnh ở màn cuối, đặt ngay trước lời chúc.
- Ngọn nến, quầng sáng và các điểm lấp lánh chuyển động nhẹ; tự tắt chuyển động khi thiết bị yêu cầu giảm hiệu ứng.
- Giữ nguyên toàn bộ ảnh, nội dung và luồng tương tác của V17.
# V19 — Smooth Love

- Căn lại lời kết song ngữ thành hai tầng rõ ràng: “Anh yêu em.” và “Jag älskar dig.”
- Tách 7 ảnh gốc khỏi HTML để giảm file trang từ hơn 11 MB xuống khoảng 300 KB.
- Tắt các lớp noise/blur nặng, tạm dừng hiệu ứng trang trí khi cuộn và khi tab không hoạt động.
- Giữ nguyên ảnh, bánh sinh nhật, nội dung và luồng tương tác của V18.

# V20 — Golden Hour Soundscape

- Thêm “Golden Hour in Your Eyes 2” làm nhạc nền chính thức của món quà.
- Nhạc tự bắt đầu sau thao tác mở khóa, lặp liên tục bằng cơ chế loop gốc của trình duyệt.
- Khi phát lời chúc, nhạc nền tự hạ nhỏ và tăng trở lại bằng chuyển âm lượng mượt.
- Tối ưu nhạc nền xuống MP3 128 kbps để giảm thời gian tải mà vẫn đủ chất lượng nghe nền.

# V21 — Embedded Music Fix

- Nhúng trực tiếp toàn bộ nhạc nền vào `index.html` để trang vẫn phát nhạc khi mở local.
- Không còn phụ thuộc vào đường dẫn tới file MP3 nằm cạnh trang.
- Giữ nguyên loop liên tục và cơ chế tự hạ nhạc khi lời chúc bắt đầu.

# V22 — Continuous Music & Smooth Finale

- Nhạc nền tiếp tục chạy xuyên suốt khi mở đoạn điện ảnh và dòng kết cuối cùng.
- Nhạc chỉ hạ nhỏ khi phát lời chúc bằng giọng nói, sau đó tự trở lại mức nền.
- Thanh “Giữ để mở” chuyển sang transform trên GPU, không còn thay đổi layout từng khung hình.
- Chuẩn bị sẵn màn cuối trước khi cuộn tới và giảm tải confetti để chuyển cảnh mượt hơn.
- Giữ nguyên giao diện, ảnh, nội dung và các hiệu ứng trang trí của V21.

# V23 Final — Mobile

- Nhúng trực tiếp giọng thật của Hoàng vào trang, không còn dùng giọng máy dự phòng.
- Giọng được giảm tiếng phòng, cân động và chuẩn hóa âm lượng; nhạc vẫn chạy nhỏ phía sau.
- Ép chế độ mobile tối ưu ngay khi mở, không phụ thuộc nhận diện thiết bị.
- Chuyển bảy ảnh sang WebP tối ưu màn hình điện thoại, giảm mạnh tải ảnh nhưng giữ nguyên bố cục.
- Giảm các lớp dựng hình tốn tài nguyên; giữ nguyên nội dung và cảm giác của hiệu ứng chính.

# V24 Final — Recorded Voice Reliability Fix

- Sửa trường hợp file ghi âm đã tải xong trước khi trình duyệt kịp gắn sự kiện `loadedmetadata`/`canplay`.
- Kiểm tra trực tiếp trạng thái audio ngay khi trang khởi tạo và buộc phát giọng thật từ thao tác chạm.
- Không còn tự chuyển sang giọng đọc máy khi bản ghi đã hiện diện.
- Giữ nguyên giao diện, ảnh, nhạc nền, nội dung và toàn bộ luồng tương tác của V23.

# V25 Final — Voice Focus Mix

- Khi bản ghi giọng thật bắt đầu phát, nhạc nền tự hạ xuống khoảng 10% mức bình thường.
- Khi người nghe bấm dừng, tạm dừng hoặc nghe hết lời chúc, nhạc nền chuyển mượt về mức cũ.
- Gia cố sự kiện `play` để nhạc luôn được hạ đúng mức dù lời chúc được mở ở thời điểm nào trong hiệu ứng lá thư.
- Giữ nguyên giao diện, ảnh, nội dung và mọi phần còn lại của V24.

# V26 Final — iPhone Web Audio Ducking

- Chuyển nhạc nền qua `AudioContext` và `GainNode` ngay từ thao tác nghe lời chúc.
- Khắc phục giới hạn iOS/Safari không cho JavaScript thay đổi `HTMLMediaElement.volume`.
- Khi giọng thật phát, nhạc được hạ bằng bộ trộn Web Audio; khi dừng hoặc nghe hết, gain trở về mức bình thường.
- Giữ cơ chế `audio.volume` làm dự phòng cho trình duyệt không hỗ trợ Web Audio.
- Giữ nguyên toàn bộ giao diện, ảnh, nội dung và luồng tương tác của V25.

# V27 Final — Letter & Voice Mix

- Hạ nhạc ngay khi bấm “Mở lá thư” bằng Web Audio trên iPhone/Safari.
- Khi phát giọng thật, giữ nhạc nền ở mức nhẹ `.04`, cao hơn một chút so với V26 nhưng vẫn ưu tiên lời chúc.
- Khi dừng hoặc nghe hết bản ghi, nhạc trở lại mức bình thường.
- Giữ nguyên giao diện, nội dung và toàn bộ phần còn lại của V26.

# V28 Final — Balanced Dual-Audio Mix

- Đưa cả nhạc nền và giọng thu vào cùng một `AudioContext` với hai `GainNode` riêng.
- Tăng nhạc nền trong lúc nghe lời chúc từ `.04` lên `.065` để nghe rõ nhẹ nhàng song song.
- Hạ riêng giọng thu xuống `.84` để mềm hơn mà vẫn là âm thanh chính.
- Nếu iPhone tự pause nhạc khi giọng bắt đầu hoặc kết thúc, trang tự phát lại và khôi phục mức nhạc bình thường.
- Tôn trọng thao tác “Tắt nhạc” của người nghe và giữ nguyên mọi phần còn lại của V27.
